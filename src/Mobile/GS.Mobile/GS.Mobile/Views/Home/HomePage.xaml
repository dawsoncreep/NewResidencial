<?xml version="1.0" encoding="utf-8" ?>
<local:HomeWrapper xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GS.Mobile.Views.Wrappers;assembly=GS.Mobile"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviorsPack="clr-namespace:Xamarin.Forms.BehaviorsPack;assembly=Xamarin.Forms.BehaviorsPack"
             mc:Ignorable="d"
             x:Class="GS.Mobile.Views.HomePage">

    <ContentPage.Behaviors>
        <behaviorsPack:EventToCommandBehavior EventName="Appearing" Command="{Binding OnLoadCommand}"/>
    </ContentPage.Behaviors>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding PageTitle}" d:Text="Just a Sample"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Click!!" Command="{Binding DoSomething}"/>
        </StackLayout>
    </ContentPage.Content>
</local:HomeWrapper>